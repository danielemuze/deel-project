apiVersion: v1
kind: Service
metadata:
  name: {{ include "reverse-ip-app-chart.fullname" . }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
  selector:
    app: {{ include "reverse-ip-app-chart.name" . }}
